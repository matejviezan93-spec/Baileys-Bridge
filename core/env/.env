# Placeholder environment configuration shared across Baileys-Bridge modules
# Duplicate values as needed for local orchestration.
APP_ENV=development
POSTGRES_HOST=bridge_sql
POSTGRES_PORT=5432
API_BASE_URL=http://api_server:8000
